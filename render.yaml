services:
  - type: web
    name: telegram-silent-forward-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python forward_bot_silent.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.7
